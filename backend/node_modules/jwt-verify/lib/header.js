exports.extract = function (req, headerName, next) {
	if (!req || !req.header(headerName)) {
		return next(new Error('Missing HTTP Header Authorization from the request'));
	}
	next(null, req.header(headerName));
}
